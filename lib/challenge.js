'use strict'

// This function will be called with 3 arguments set to
// 2, 10, and 1000000 respectively
const doubleBasePalindromeSum = function (/* base1, base2, upperLimit */) {
}

let sum=0
for (let i = 1; i < 1000000; i++) {
  if (numbers.isPalindrome(i.toString(10)) && numbers.isPalindrome(i.toString(2)))
    sum += i
}

// to be honest i really didnt know where to begin with this. had to look online
// and was still not sure.  Will review the Euler project in general to get a
// better understanding

module.exports = {
  doubleBasePalindromeSum
}
